node_modules/
dist/
.DS_Store
*.log

# build scratch (the mount won't let me delete these; git will just ignore them)
tsconfig.tsbuildinfo
vite.config.ts.timestamp-*.mjs
src/styles.css

# Capacitor regenerates the native projects on the CI runners
ios/
android/

# never commit these
*.p12
*.p8
*.mobileprovision
.env
.env.local

# Apple signing material - NEVER commit any of this
certs/
*.certSigningRequest
*.cer
*.pem
*.key
*.txt
